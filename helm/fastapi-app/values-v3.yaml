# values-v2.yaml — the new "green" release (v2)
# Commit this file and update argocd/application.yaml to reference it
# to trigger a blue-green rollout.
image:
  tag: "v3.0.0"

app:
  color: "yellow"
  version: "v3.0.0"
